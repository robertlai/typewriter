// Generated by CoffeeScript 1.10.0
var stocksApp;

stocksApp = angular.module('drawingApp', []);

stocksApp.controller('drawingController', function($scope) {
  var BADNESS, CHARHEIGHT, CHARWIDTH, SPACEWIDTH, drawLetter, globX, globY;
  $scope.text = '';
  globX = 30;
  globY = 50;
  CHARWIDTH = 30;
  CHARHEIGHT = 50;
  SPACEWIDTH = 10;
  BADNESS = 2;
  $scope.update = function() {
    var points;
    points = [[20, 30], [10, 30], [10, 45], [20, 45], [20, 30], [20, 57]];
    return drawLetter(points);
  };
  $scope.doClick = function(event) {
    var canvasElement, ctx, offsetX, offsetY, x, y;
    x = event.clientX;
    y = event.clientY;
    offsetX = event.offsetX;
    offsetY = event.offsetY;
    canvasElement = document.getElementById('canvas');
    ctx = canvasElement.getContext('2d');
    ctx.beginPath();
    ctx.arc(x, y, 10, 0, Math.PI * 2, true);
    ctx.closePath();
    return ctx.fill();
  };
  return drawLetter = function(points) {
    var canvasElement, ctx, i, j, ref, xShift, yShift;
    canvasElement = document.getElementById('canvas');
    ctx = canvasElement.getContext('2d');
    for (i = j = 0, ref = points.length; 0 <= ref ? j < ref : j > ref; i = 0 <= ref ? ++j : --j) {
      xShift = (Math.random() - 0.5) * BADNESS;
      yShift = (Math.random() - 0.5) * BADNESS;
      points[i][0] += xShift + globX;
      points[i][1] += yShift + globY;
      if (i === 0) {
        ctx.moveTo(points[i][0], points[i][1]);
      } else {
        ctx.lineTo(points[i][0], points[i][1]);
      }
    }
    return ctx.stroke();
  };
});
