// Generated by CoffeeScript 1.9.3
var stocksApp;

stocksApp = angular.module('drawingApp', []);

stocksApp.controller('drawingController', function($scope) {
  $scope.text = '';
  $scope.update = function() {
    return console.log($scope.text);
  };
  return $scope.doClick = function(event) {
    var canvasElement, ctx, offsetX, offsetY, x, y;
    x = event.clientX;
    y = event.clientY;
    offsetX = event.offsetX;
    offsetY = event.offsetY;
    canvasElement = document.getElementById('canvas');
    ctx = canvasElement.getContext('2d');
    ctx.beginPath();
    ctx.arc(x, y, 10, 0, Math.PI * 2, true);
    ctx.closePath();
    return ctx.fill();
  };
});
